通用约束
项目路径：C:\Users\云发鹏🐧\Desktop\M5
主程序：arduino_demos/04_camera_pet_battle/04_camera_pet_battle.ino
公共头文件：
pet_model.h
vision_types.h
ui_types.h
battle_protocol.h
不要重新定义公共类型。
不要私自修改公共头文件字段、枚举值、UDP 包结构。
不修改 BattlePetPacket。
不恢复语音识别。
不引入新依赖。
不写死云端 API key。
当前必须保持 CoreS3 本地离线可运行。
未来 SD 卡、App、云端大模型、社区生态只能写成预留或规划，不能写成当前已实现。
如需公共接口变更，先输出：

接口变更建议：
- 需要修改的头文件：
- 当前接口：
- 建议改动：
- 改动原因：
- 对存档/通信/编译的影响：
- 是否需要版本号升级：
- 兼容方案：

4. 通信、烧录与硬件验证（2 小时）
任务提示词：

你负责 CoreS3 双板通信、MATCH 稳定性、最终编译、烧录和硬件验证。

目标：
1. 双方只要都打开 MATCH，就能自动发现、连接并进入对战。
2. 玩家界面不要出现 HOST / CLIENT 概念。
3. 底层如果必须选主从，只能作为内部实现细节。
4. 任意两块板子都能配对，不依赖写死 MAC。
5. 通信断开后能自动回到 MATCHING 或重试。
6. 保持本地离线，不依赖路由器、服务器或手机 App。
7. 负责最终编译、烧录和双板实机验证。

需求边界：
- 不直接修改 BattlePetPacket。
- 如果需要新增握手包、心跳包、ACK、sessionId、状态机，先输出接口变更建议。
- 不改变宠物生成、战斗机制、UI 美术细节。
- UI 只需要提供通信状态给 UI 模块使用。
- 不引入新依赖。

建议方向：
- 设计通信状态机：DISCOVERING、PAIRING、READY、BATTLING、LOST。
- 两块板都开启 AP+STA。
- 扫描到 M5PET 设备后自动协商内部角色。
- 使用心跳、超时、重试、重复包过滤提高稳定性。
- 屏幕只显示 MATCHING、CONNECTED、BATTLING、RETRYING 等玩家可理解状态。

输出要求：
- 通信状态机设计。
- MATCH 配对流程。
- 断线重连策略。
- 心跳/ACK/超时策略。
- 是否需要新增协议包。
- 接口变更建议。
- 编译、烧录、双板实测记录。
验收标准：

两块板同时进入 MATCH 后能稳定发现对方。
两块板先后进入 MATCH 也能连接。
玩家不需要理解 HOST / CLIENT。
断开后能恢复到可重试状态。
最终编译通过并完成硬件验证记录。

模块间依赖关系
2. 捕捉识别与宠物生成 输出宠物生成规则，供 3. 玩家流程、背包养成与 UI 展示和养成使用。
4. 通信、烧录与硬件验证 输出通信状态，供 3. 玩家流程、背包养成与 UI 展示 MATCH/BATTLE 状态。
3. 玩家流程、背包养成与 UI 输出战斗、养成、友情需求；如需持久化字段，交给系统架构总控评估。
5. 产品化、App、云端与文档 只规划产品和文档，不直接改变固件协议；如需 App 通信，先提交接口变更建议。
所有公共接口变更统一交给系统架构总控合并。
总体验收标准
主程序仍能编译。
公共类型没有重复定义。
UDP 对战包结构未被破坏。
捕捉、背包、放生、养成、MATCH、BATTLE 基础流程仍可运行。
CoreS3 不依赖云端即可完整体验本地玩法。
文档准确区分“当前已实现”和“未来规划”。

当前烧录交付输入（2026-06-01）

烧录主程序：
arduino_demos/04_camera_pet_battle/04_camera_pet_battle.ino

建议编译命令：
.\scripts\compile-demo.ps1 -Sketch .\arduino_demos\04_camera_pet_battle\04_camera_pet_battle.ino -BuildRoot C:\tmp\m5_arduino_build_full_flash_ready

最近一次本地编译记录：
- 程序空间：1607857 bytes（51%）。
- 全局变量：69268 bytes（21%）。
- 记录位置：release/oshw/verification-log.csv。

已放入当前项目的可交付模块成果：
- 捕捉识别与宠物生成：限定 8 类识别、失败门控、ObjectClass 到五行/物种映射、PetGenes 外观生成、45 个兼容视觉变体、样本训练脚本和 vision_model_data.h。
- 采样与调试：采样模式、实时质量仪表、/app 识别调试面板、样本 manifest 导出、burst 候选帧调试。
- 玩家流程与 UI：IDLE、PHOTO、WILD、CAPTURE_FAIL、BAG、MATCH、BATTLE、RELEASE_CONFIRM 流程，背包、放生确认、养成状态、图鉴。
- 本地 HTTP 验收入口：/api/v1/status、/api/v1/storage、/api/v1/sampling、/api/v1/samples、/api/v1/samples/manifest、/api/v1/samples/file。
- 串口验收入口：STATUS、ACCEPTANCE、BAGSTATUS、SAMPLE、EDGE_HINT、HUSKY_HINT、SDINFO、SDPUT。
- 电脑和手机网页入口：电脑使用 http://127.0.0.1:8790/；手机使用 http://<电脑局域网 IPv4>:8791/。桥接网页含 AI 周边工坊、2D 形象、语音文件和本地资产保存，但不写入固件 API key。
- SD 可选资源：声音包、皮肤 palette、动作 profile、资源 manifest 和 validate-sd-payload 校验脚本。
- 对战增强：技能显示、三回合对战流程、RAM 回放日志、外部识别 hint 保持协议兼容。
- 玩家可见对战分数面板使用“我方/对手”，不显示 HOST/CLIENT。
- 实验分支：Edge Impulse/FOMO adapter、HuskyLens bridge、ESP-IDF + ESP-DL/ESP-WHO PoC skeleton。
- 发布材料：OSHWHub/OSHWHLab release kit、Hugging Face 数据集卡草稿、脱敏 hf-dataset-manifest.csv、delivery-notes。

烧录模块需要继续实测并填写：
- 烧录端口、flash_ok。
- /app 手机访问、/api/v1/status、/api/v1/storage、/api/v1/samples。
- 串口 ACCEPTANCE、STATUS、BAGSTATUS、SAMPLE status、SDINFO 输出。
- SD 卡挂载、声音/皮肤/动作资源回退。
- 白墙、白纸、桌面、反光、暗光 negative 捕捉。
- 明确居中物体 positive 捕捉。
- 单板 MATCH/BATTLE 基础流程。
- 双板 MATCH/BATTLE 流程。

不需要烧录模块处理的事项：
- 不修改 pet_model.h、vision_types.h、ui_types.h、battle_protocol.h。
- 不修改 BattlePetPacket、背包存储格式或 UDP 包结构。
- 不接入云端 API，不恢复语音识别，不新增依赖。
- Hugging Face 公开上传仍需样本 license/privacy 审核和更多 CoreS3 实拍样本。
