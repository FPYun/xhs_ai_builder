通用约束
项目路径：C:\Users\云发鹏🐧\Desktop\M5
主程序：arduino_demos/04_camera_pet_battle/04_camera_pet_battle.ino
公共头文件：
pet_model.h
vision_types.h
ui_types.h
battle_protocol.h
不要重新定义公共类型。
不要私自修改公共头文件字段、枚举值、UDP 包结构。
不修改 BattlePetPacket。
不恢复语音识别。
不引入新依赖。
不写死云端 API key。
当前必须保持 CoreS3 本地离线可运行。
未来 SD 卡、App、云端大模型、社区生态只能写成预留或规划，不能写成当前已实现。
如需公共接口变更，先输出：

接口变更建议：
- 需要修改的头文件：
- 当前接口：
- 建议改动：
- 改动原因：
- 对存档/通信/编译的影响：
- 是否需要版本号升级：
- 兼容方案：
2. 捕捉识别与宠物生成（1 小时）
任务提示词：

你负责 CoreS3 五行宠物项目中的拍照识别、捕捉判断和宠物生成逻辑。

目标：
1. 完善拍照、图像识别、捕捉失败判断流程。
2. 优化 ImageTraits、RecognitionResult 到 PetGenes 的生成链路。
3. 根据图像识别结果生成对应宠物，而不是明显随机生成。
4. 固定 ObjectClass 到五行元素和物种倾向的主映射。
5. 让 ImageTraits 只影响体型、颜色、花纹、情绪等外观细节。
6. 识别失败时不生成随机宠物。
7. 保持本地离线识别，不恢复语音识别，不接入云端 API。

需求边界：
- 不重新定义 PetGenes、ImageTraits、RecognitionResult、SavedPet。
- 不私自修改 pet_model.h 或 vision_types.h。
- 不修改通信协议。
- 不引入新依赖。
- 如果需要新增稀有度、宠物 ID、技能、亲密度等字段，先输出接口变更建议。

输出要求：
- ObjectClass 到元素/物种的映射表。
- 宠物生成逻辑调整方案。
- 捕捉失败条件说明。
- 升级和对战成长可衔接的建议。
- 是否需要公共接口变更。
验收标准：

同一识别类别稳定生成对应元素和合理物种倾向。
时间、拍照次数、随机扰动不能覆盖识别类别。
识别失败不会生成随机宠物。
捕捉、背包、放生流程不被破坏。

模块间依赖关系
2. 捕捉识别与宠物生成 输出宠物生成规则，供 3. 玩家流程、背包养成与 UI 展示和养成使用。
4. 通信、烧录与硬件验证 输出通信状态，供 3. 玩家流程、背包养成与 UI 展示 MATCH/BATTLE 状态。
3. 玩家流程、背包养成与 UI 输出战斗、养成、友情需求；如需持久化字段，交给系统架构总控评估。
5. 产品化、App、云端与文档 只规划产品和文档，不直接改变固件协议；如需 App 通信，先提交接口变更建议。
所有公共接口变更统一交给系统架构总控合并。
总体验收标准
主程序仍能编译。
公共类型没有重复定义。
UDP 对战包结构未被破坏。
捕捉、背包、放生、养成、MATCH、BATTLE 基础流程仍可运行。
CoreS3 不依赖云端即可完整体验本地玩法。
文档准确区分“当前已实现”和“未来规划”。